
sarojsanyasi

Practical-2
Edit


DBS302 – Practical Guide Set 2
Implementing Leaderboard, Real-Time Analytics, and Geo-Search with Redis

This guide assumes students have basic familiarity with Node.js or Python, but all core commands are shown using the Redis CLI and a simple Python client for clarity.

Common Setup for All Practicals (1A, 1B, 1C)
AIM
To prepare a consistent development and execution environment for implementing Redis-based leaderboard, analytics, and geo-search features using advanced Redis data structures.

Objectives
By the end of this common setup, the student will be able to:

Install and run a local Redis server and connect via CLI.
Connect to Redis from a Python client script.
Verify support for sorted sets, bitmaps, HyperLogLog, and geospatial commands in the installed Redis version.
Prerequisites
Module: DBS302 – NoSQL Database Management (Unit II: Redis).
Tools installed:
Redis server (version 7.x or later recommended).
Python 3.10+ (or Node.js – this guide uses Python).
redis Python client (pip install redis).
Step 1: Start Redis Server
On Linux/macOS:

redis-server
On Windows (using WSL or Redis for Windows package):

redis-server
Verify that Redis is running:

redis-cli ping
# Expected output: PONG
Step 2: Create a Working Directory
mkdir redis-practicals
cd redis-practicals
Create a virtual environment (optional but recommended):

python -m venv venv
source venv/bin/activate    # On Windows: venv\Scripts\activate
pip install redis
Step 3: Test Python–Redis Connectivity
Create test_redis_connection.py:

import redis

# Create Redis client (defaults to localhost:6379)
r = redis.Redis(host="127.0.0.1", port=6379, db=0, decode_responses=True)

def main():
    # Simple health check
    pong = r.ping()
    print(f"Redis PING response: {pong}")

    # Basic SET/GET
    r.set("dbs302:test:key", "hello-redis")
    value = r.get("dbs302:test:key")
    print(f"Value from Redis: {value}")

if __name__ == "__main__":
    main()
Run:

python test_redis_connection.py
Expected output:

Redis PING response: True
Value from Redis: hello-redis
If this works, the environment is ready for all three practicals.

Practical–1A: Leaderboard with Redis Sorted Sets
1. Metadata
AIM
To implement a real-time leaderboard system using Redis sorted sets to efficiently store, update, and query player rankings.

Objectives
After completing Practical–1A, the student will be able to:

Explain the characteristics and use cases of Redis sorted sets (ZSET).
Implement a leaderboard for a game or competitive application using ZADD, ZREVRANGE, ZREVRANK, and ZINCRBY.
Design API-like functions (in Python) to encapsulate leaderboard operations following good software engineering practices.
Experiment with pagination and “around-me” queries for a more realistic leaderboard.
Mapping to Module Outcomes
DBS302 LO3: Implement CRUD operations and data structures in Redis.
Unit II 2.2, 2.3: Redis data types (sorted sets) and data structures.
2. Conceptual Overview
2.1 What is a Sorted Set in Redis?
A sorted set is a collection of unique members, each associated with a numeric score.
Members are ordered by score; reads and updates are logarithmic in complexity, making them suitable for large leaderboards.
Analogy:
Think of a sports ranking board displayed in a stadium — each player (member) has a score, and the board automatically keeps them sorted by score.

Key commands used:

ZADD key score member – Add/update a member with a given score.
ZREVRANGE key start stop WITHSCORES – Get members in descending score order.
ZREVRANK key member – Get rank of a member (0-based, best is 0).
ZINCRBY key increment member – Increase score of a member.
3. Step-by-Step Practical Procedure
3.1 Design the Leaderboard
Scenario:
A simple online game keeps scores for players. The leaderboard should support:

Adding players with initial scores.
Updating scores as players play.
Getting top N players.
Getting rank and score of a particular player.
Getting a “page” of the leaderboard (for pagination).
Define a leaderboard key naming convention:

Key pattern: leaderboard:{name}
Example: leaderboard:game:season1
3.2 Quick Demo Using Redis CLI
Open the CLI:

redis-cli
Use:

# Add players
ZADD leaderboard:game:season1 1500 alice
ZADD leaderboard:game:season1 2300 bob
ZADD leaderboard:game:season1 1800 charlie
ZADD leaderboard:game:season1 2100 diana
ZADD leaderboard:game:season1 1950 eve

# Get top 3 players (highest score first)
ZREVRANGE leaderboard:game:season1 0 2 WITHSCORES

# Get rank of a player (0-based; add 1 in application)
ZREVRANK leaderboard:game:season1 charlie

# Get score of a player
ZSCORE leaderboard:game:season1 charlie

# Increment score of a player
ZINCRBY leaderboard:game:season1 500 charlie

# Check updated top 3
ZREVRANGE leaderboard:game:season1 0 2 WITHSCORES
Students should observe how ranking changes when scores are updated.

4. Implementation in Python (Recommended Demo)
Create leaderboard.py:

import redis
from typing import List, Dict, Optional, Tuple

class Leaderboard:
    """
    Leaderboard backed by Redis sorted sets.
    Encapsulates all leaderboard-related operations.
    """

    def __init__(self, name: str, redis_client: Optional[redis.Redis] = None) -> None:
        self.name = name
        self.key = f"leaderboard:{name}"
        self.r = redis_client or redis.Redis(
            host="127.0.0.1",
            port=6379,
            db=0,
            decode_responses=True,
        )

    def add_score(self, player_id: str, score: float) -> int:
        """
        Add or update a player's score.
        Returns the player's new rank (1-based).
        """
        # ZADD will insert or update the member's score
        self.r.zadd(self.key, {player_id: score})
        return self.get_rank(player_id)

    def increment_score(self, player_id: str, delta: float) -> Tuple[float, int]:
        """
        Increment a player's score.
        Returns (new_score, new_rank).
        """
        new_score = self.r.zincrby(self.key, delta, player_id)
        new_rank = self.get_rank(player_id)
        return new_score, new_rank

    def get_rank(self, player_id: str) -> Optional[int]:
        """
        Get player's rank (1-based). Returns None if player is not found.
        """
        rank_zero_based = self.r.zrevrank(self.key, player_id)
        if rank_zero_based is None:
            return None
        # Convert 0-based rank to 1-based
        return rank_zero_based + 1

    def get_score(self, player_id: str) -> Optional[float]:
        """
        Get player's current score.
        """
        score = self.r.zscore(self.key, player_id)
        return float(score) if score is not None else None

    def get_top(self, n: int = 10) -> List[Dict]:
        """
        Get top N players.
        """
        results = self.r.zrevrange(self.key, 0, n - 1, withscores=True)
        return [
            {"rank": i + 1, "player": player, "score": score}
            for i, (player, score) in enumerate(results)
        ]

    def get_page(self, page: int, page_size: int = 10) -> List[Dict]:
        """
        Get a specific page of the leaderboard.
        Page is 1-based.
        """
        if page < 1:
            raise ValueError("page must be >= 1")

        start = (page - 1) * page_size
        end = start + page_size - 1
        results = self.r.zrevrange(self.key, start, end, withscores=True)
        return [
            {"rank": start + i + 1, "player": player, "score": score}
            for i, (player, score) in enumerate(results)
        ]

    def get_around_player(self, player_id: str, radius: int = 2) -> List[Dict]:
        """
        Get players around a specific player (for 'around me' views).
        Includes 'radius' players above and below the given player.
        """
        rank_zero_based = self.r.zrevrank(self.key, player_id)
        if rank_zero_based is None:
            return []

        start = max(0, rank_zero_based - radius)
        end = rank_zero_based + radius
        results = self.r.zrevrange(self.key, start, end, withscores=True)

        return [
            {"rank": start + i + 1, "player": player, "score": score}
            for i, (player, score) in enumerate(results)
        ]

    def count_players(self) -> int:
        """
        Get total number of players in the leaderboard.
        """
        return self.r.zcard(self.key)

    def remove_player(self, player_id: str) -> bool:
        """
        Remove a player from the leaderboard.
        Returns True if the player was removed, False otherwise.
        """
        removed = self.r.zrem(self.key, player_id)
        return removed > 0


def demo():
    """
    Demonstrate basic usage of the Leaderboard class.
    """
    lb = Leaderboard("game:season1")

    # Clear previous data for a clean demo
    lb.r.delete(lb.key)

    # Add players
    print("Adding initial scores...")
    lb.add_score("alice", 1500)
    lb.add_score("bob", 2300)
    lb.add_score("charlie", 1800)
    lb.add_score("diana", 2100)
    lb.add_score("eve", 1950)

    # Print top 3
    print("\nTop 3 players:")
    for entry in lb.get_top(3):
        print(f" #{entry['rank']} {entry['player']}: {entry['score']}")

    # Show Charlie's rank and score
    print("\nCharlie's current rank and score:")
    print(" Rank:", lb.get_rank("charlie"))
    print(" Score:", lb.get_score("charlie"))

    # Increment Charlie's score
    print("\nIncrementing Charlie's score by 500...")
    new_score, new_rank = lb.increment_score("charlie", 500)
    print(f" New score: {new_score}, new rank: {new_rank}")

    # Players around Charlie
    print("\nPlayers around Charlie:")
    for entry in lb.get_around_player("charlie", radius=2):
        print(f" #{entry['rank']} {entry['player']}: {entry['score']}")

    # Page 1 of leaderboard
    print("\nPage 1 of leaderboard (page_size=3):")
    for entry in lb.get_page(page=1, page_size=3):
        print(f" #{entry['rank']} {entry['player']}: {entry['score']}")


if __name__ == "__main__":
    demo()
Sample Output (Illustrative)
Adding initial scores...

Top 3 players:
 #1 bob: 2300.0
 #2 diana: 2100.0
 #3 eve: 1950.0

Charlie's current rank and score:
 Rank: 4
 Score: 1800.0

Incrementing Charlie's score by 500...
 New score: 2300.0, new rank: 1

Players around Charlie:
 #1 charlie: 2300.0
 #2 bob: 2300.0
 #3 diana: 2100.0

Page 1 of leaderboard (page_size=3):
 #1 charlie: 2300.0
 #2 bob: 2300.0
 #3 diana: 2100.0
5. Exercises and Mini Case Study
Short Exercises
Modify the leaderboard to support “daily” and “all-time” leaderboards using keys like leaderboard:game:daily:2026-03-17 and leaderboard:game:alltime.
Add a method get_players_in_score_range(min_score, max_score) using ZREVRANGEBYSCORE.
Add TTL to the daily leaderboard key, so it expires after 7 days.
Mini Case Study
A startup is building an online quiz application. They need:

A real-time global leaderboard.
Country-wise leaderboards (e.g., leaderboard:quiz:country:BT).
Task: Ask students to design the key schema and required methods on the Leaderboard class to support country-specific leaderboards.

Common Mistakes (and How to Avoid Them)
Confusing ZRANGE (ascending) with ZREVRANGE (descending). Encourage students to always check the desired ordering.
Forgetting that ZREVRANK is 0-based; they should adjust to 1-based ranks in the application layer.
Using random string keys without a clear naming convention; insist on a clear prefix pattern.
Quick Revision Points
Sorted sets provide automatically ordered collections based on scores.
Use ZADD to insert/update and ZINCRBY to increment scores.
Use ZREVRANGE for top-N and ZREVRANK for rank calculation.
Encapsulate Redis calls in a class for maintainability and testability.
Practical–1B: Real-Time Analytics with Bitmaps and HyperLogLog
1. Metadata
AIM
To implement real-time user analytics using Redis bitmaps for activity tracking and HyperLogLog for unique user counting.

Objectives
After completing Practical–1B, the student will be able to:

Describe how Redis bitmaps and HyperLogLog work conceptually and their trade-offs.
Implement daily active user tracking using bitmaps (SETBIT, GETBIT, BITCOUNT).
Implement approximate unique visitor counting using HyperLogLog (PFADD, PFCOUNT).
Design and code a small analytics service that exposes high-level methods for these metrics.
Mapping to Module Outcomes
DBS302 LO5: Assess performance implications of different NoSQL data structures.
Unit II 2.3.1, 2.3.2: Bitmaps and HyperLogLog for analytics.
2. Conceptual Overview
2.1 Bitmaps
A bitmap uses bits in a string to represent yes/no (binary) states, typically one bit per user or per event.
Example use: daily active users (DAU) – bit at position user_id is 1 if user is active that day.
Key commands:

SETBIT key offset value
GETBIT key offset
BITCOUNT key – Count bits set to 1.
2.2 HyperLogLog (HLL)
HyperLogLog is a probabilistic data structure that estimates the number of unique elements (cardinality) with very low memory usage and an acceptable error rate.
Example use: approximate count of daily unique visitors (UV).
Key commands:

PFADD key element [element ...]
PFCOUNT key – Get approximate cardinality.
Analogy:
Think of HLL as a “smart bucket” that does not remember exactly who entered, but can accurately estimate how many unique people have entered.

3. Step-by-Step Practical Procedure
Scenario:
A web application wants to track metrics such as:

Daily Active Users (DAU) using bitmaps.
Daily Unique Visitors (UV) using HyperLogLog.
Assume user_id is an integer in a reasonable range (e.g., 0–1,000,000).

3.1 CLI Demo – Bitmaps
redis-cli
# Simulate activity on 2026-03-17
SETBIT analytics:active:2026-03-17 1 1   # user_id=1 active
SETBIT analytics:active:2026-03-17 42 1  # user_id=42 active
SETBIT analytics:active:2026-03-17 100 1 # user_id=100 active

# Check if user 42 was active
GETBIT analytics:active:2026-03-17 42

# Count active users for the day
BITCOUNT analytics:active:2026-03-17
3.2 CLI Demo – HyperLogLog
# Add visits for 2026-03-17
PFADD analytics:uv:2026-03-17 user1 user2 user3
PFADD analytics:uv:2026-03-17 user2 user3 user4

# Get approximate unique visitors
PFCOUNT analytics:uv:2026-03-17
4. Implementation in Python (Analytics Service)
Create analytics.py:

import redis
from typing import Optional


class RealtimeAnalytics:
    """
    Real-time analytics using Redis bitmaps and HyperLogLog.
    Tracks:
      - Daily Active Users (DAU) via bitmaps.
      - Daily Unique Visitors (UV) via HyperLogLog.
    """

    def __init__(self, redis_client: Optional[redis.Redis] = None) -> None:
        self.r = redis_client or redis.Redis(
            host="127.0.0.1",
            port=6379,
            db=0,
            decode_responses=True,
        )

    # --------------------
    # Bitmap-based metrics
    # --------------------
    def _dau_key(self, date_str: str) -> str:
        """
        Build key for daily active user bitmap.
        Example date_str: '2026-03-17'.
        """
        return f"analytics:dau:{date_str}"

    def mark_user_active(self, date_str: str, user_id: int) -> None:
        """
        Mark a user as active for a given day using SETBIT.
        """
        if user_id < 0:
            raise ValueError("user_id must be non-negative")

        key = self._dau_key(date_str)
        # Set bit at position = user_id
        self.r.setbit(key, user_id, 1)

    def is_user_active(self, date_str: str, user_id: int) -> bool:
        """
        Check whether the user was active on a given day using GETBIT.
        """
        key = self._dau_key(date_str)
        bit_value = self.r.getbit(key, user_id)
        return bit_value == 1

    def count_daily_active_users(self, date_str: str) -> int:
        """
        Count daily active users for the given date using BITCOUNT.
        """
        key = self._dau_key(date_str)
        return self.r.bitcount(key)

    # ------------------------
    # HyperLogLog-based metrics
    # ------------------------
    def _uv_key(self, date_str: str) -> str:
        """
        Build key for daily unique visitors HyperLogLog.
        """
        return f"analytics:uv:{date_str}"

    def add_visit(self, date_str: str, user_identifier: str) -> None:
        """
        Add a visit for a given day in HyperLogLog.
        user_identifier can be a user_id, session_id, or IP string.
        """
        key = self._uv_key(date_str)
        self.r.pfadd(key, user_identifier)

    def count_unique_visitors(self, date_str: str) -> int:
        """
        Get approximate number of unique visitors for the given date using PFCOUNT.
        """
        key = self._uv_key(date_str)
        return self.r.pfcount(key)


def demo():
    analytics = RealtimeAnalytics()

    date = "2026-03-17"

    # Clear previous demo data
    analytics.r.delete(analytics._dau_key(date))
    analytics.r.delete(analytics._uv_key(date))

    # Simulate some activity
    print(f"Simulating activity for {date}...")

    # Users 1, 42, 100 are active
    analytics.mark_user_active(date, 1)
    analytics.mark_user_active(date, 42)
    analytics.mark_user_active(date, 100)

    # Visits (note repeated identifiers)
    analytics.add_visit(date, "user1")
    analytics.add_visit(date, "user2")
    analytics.add_visit(date, "user3")
    analytics.add_visit(date, "user2")  # duplicate
    analytics.add_visit(date, "user3")  # duplicate
    analytics.add_visit(date, "user4")

    # Check a user's activity
    print("\nIs user 42 active?")
    print(" ->", analytics.is_user_active(date, 42))

    # Daily active users (exact, from bitmap)
    dau = analytics.count_daily_active_users(date)
    print("\nDaily Active Users (DAU):", dau)

    # Unique visitors (approximate, from HyperLogLog)
    uv = analytics.count_unique_visitors(date)
    print("Unique Visitors (UV) [approx]:", uv)


if __name__ == "__main__":
    demo()
Sample Output (Illustrative)
Simulating activity for 2026-03-17...

Is user 42 active?
 -> True

Daily Active Users (DAU): 3
Unique Visitors (UV) [approx]: 4
5. Exercises, Use Cases, and Pitfalls
Use Case Extension
For a streaming platform:

Use bitmaps to track which users watched at least one video per day.
Use HyperLogLog to track approximate unique viewers per show.
Students can design keys like:

analytics:dau:2026-03-17
analytics:uv:show:{show_id}:2026-03-17
Short Exercises
Add a method merge_uv(date_str_list) that merges multiple HyperLogLogs into a weekly UV key using PFMERGE. [web:7][web:13]
Implement an API to compute “stickiness” ratio = DAU / MAU, where MAU (Monthly Active Users) is derived by merging 30 daily HLL keys.
Write a small CLI script that prints DAU and UV for a given date passed as a command-line argument.
Common Mistakes
Using bitmaps for user IDs that are extremely sparse (e.g., very large user IDs) – this wastes memory; advise students to use local indexing or mapping.
Expecting HyperLogLog to return exact counts – remind that it is approximate but with small error (< 1–2% for typical use).
Forgetting to set TTL for keys when historical data is not required; this can cause unbounded memory growth.
Quick Revision Points
Bitmaps: perfect for boolean per-user/per-day data with constant-time updates and fast aggregate counts. [web:7][web:10]
HyperLogLog: approximate unique counts with tiny memory footprint; ideal when exact per-user storage is not necessary. [web:13]
Both structures are write-efficient and fit real-time dashboards and counters frequently queried in web and mobile applications. [
Practical–1C: Geo-Search Functionality with Redis Geospatial Indexes
1. Metadata
AIM
To implement location-based search and distance queries using Redis geospatial indexes for storing and querying coordinates.

Objectives
After completing Practical–1C, the student will be able to:

Explain how Redis stores geospatial data and the purpose of commands like GEOADD, GEORADIUS (or GEOSEARCH), and GEODIST.
Index points of interest (e.g., stores, drivers, or users) by longitude/latitude.
Implement “find nearest locations within a radius” queries.
Write a small Python service that wraps these commands into high-level methods suitable for an application backend.
Mapping to Module Outcomes
DBS302 LO6: Design NoSQL solutions for real-world use cases (location-based services).
Unit II 2.3.4: Geospatial indexes in Redis.
2. Conceptual Overview
Redis geospatial support is implemented on top of sorted sets, where coordinates are encoded into a single score that maintains spatial ordering.

Key commands:

GEOADD key longitude latitude member – Add a location.
GEODIST key member1 member2 [unit] – Compute distance.
GEOSEARCH key FROMMEMBER member BYRADIUS radius unit [WITHDIST] [WITHCOORD] – Search within radius.
Older command GEORADIUS is still found in many examples; newer syntax uses GEOSEARCH.
Analogy:
Think of Redis as a simple in-memory map service where each point has a name, and the server can quickly answer “who is near this point?” queries.

3. Step-by-Step Practical Procedure
Scenario:
A location-based service wants to:

Store branches of a food-delivery partner in a city.
Given a user’s current location, retrieve all branches within a certain radius (e.g., 5 km).
Compute the distance between two selected branches.
3.1 CLI Demo – Adding Locations
redis-cli
Assume key: geo:stores:thimphu

# Add stores: longitude, latitude, name
GEOADD geo:stores:thimphu 89.6390 27.4728 store_norzin
GEOADD geo:stores:thimphu 89.6530 27.4712 store_changzamtog
GEOADD geo:stores:thimphu 89.6490 27.4770 store_motithang

# Distance between two stores in kilometers
GEODIST geo:stores:thimphu store_norzin store_changzamtog km
3.2 CLI Demo – Searching Nearby
Using GEOSEARCH (recommended):

# Search stores within 3 km of Norzin area store
GEOSEARCH geo:stores:thimphu FROMMEMBER store_norzin BYRADIUS 3 km WITHDIST WITHCOORD
Alternatively, using explicit coordinates:

GEOSEARCH geo:stores:thimphu FROMLONLAT 89.6390 27.4728 BYRADIUS 3 km WITHDIST WITHCOORD
Students will see a list of nearby stores with distances and coordinates.

4. Implementation in Python (Geo-Search Service)
Create geo_search.py:

import redis
from typing import List, Dict, Optional


class GeoSearchService:
    """
    Geo-search functionality using Redis geospatial indexes.
    Stores locations (e.g., stores, drivers, users) and supports nearby queries.
    """

    def __init__(self, key: str, redis_client: Optional[redis.Redis] = None) -> None:
        self.key = key  # e.g., "geo:stores:thimphu"
        self.r = redis_client or redis.Redis(
            host="127.0.0.1",
            port=6379,
            db=0,
            decode_responses=True,
        )

    def add_location(self, name: str, longitude: float, latitude: float) -> None:
        """
        Add or update a named location.
        """
        self.r.geoadd(self.key, [longitude, latitude, name])

    def distance_between(
        self,
        name1: str,
        name2: str,
        unit: str = "km",
    ) -> Optional[float]:
        """
        Compute distance between two locations in the given unit.
        Units: 'm', 'km', 'mi', 'ft'
        Returns None if one of the locations does not exist.
        """
        dist = self.r.geodist(self.key, name1, name2, unit)
        return float(dist) if dist is not None else None

    def nearby(
        self,
        longitude: float,
        latitude: float,
        radius: float,
        unit: str = "km",
        withdist: bool = True,
        withcoord: bool = True,
    ) -> List[Dict]:
        """
        Find locations within a given radius from a point.
        Uses GEOSEARCH with FROMLONLAT.
        """
        # GEOSEARCH returns list of names or tuples depending on flags;
        # redis-py uses 'geosearch' wrapper method.
        results = self.r.geosearch(
            self.key,
            longitude=longitude,
            latitude=latitude,
            radius=radius,
            unit=unit,
            withdist=withdist,
            withcoord=withcoord,
        )

        # Results structure depends on redis-py version. For decode_responses=True,
        # each row is [name, dist, [lon, lat]] if withdist and withcoord are True.
        formatted_results: List[Dict] = []
        for item in results:
            # Defensive parsing
            if isinstance(item, (list, tuple)) and len(item) >= 3:
                name = item
                dist_value = float(item)[^1]
                coord = item[^2]
                lon = float(coord)
                lat = float(coord)[^1]
                formatted_results.append(
                    {
                        "name": name,
                        "distance": dist_value,
                        "longitude": lon,
                        "latitude": lat,
                    }
                )
            else:
                # Fallback if we only have the name
                formatted_results.append({"name": str(item)})

        return formatted_results


def demo():
    service = GeoSearchService("geo:stores:thimphu")

    # Clear old data
    service.r.delete(service.key)

    # Add some demo stores in Thimphu (coordinates are illustrative)
    service.add_location("store_norzin", 89.6390, 27.4728)
    service.add_location("store_changzamtog", 89.6530, 27.4712)
    service.add_location("store_motithang", 89.6490, 27.4770)

    # Distance example
    dist = service.distance_between("store_norzin", "store_changzamtog", unit="km")
    print(f"Distance Norzin -> Changzamtog: {dist:.2f} km")

    # Nearby example: user standing at Norzin
    print("\nStores within 3 km of Norzin:")
    nearby_stores = service.nearby(89.6390, 27.4728, radius=3, unit="km")
    for store in nearby_stores:
        print(
            f" {store['name']} at ({store['latitude']:.4f}, "
            f"{store['longitude']:.4f}) – {store['distance']:.2f} { 'km' }"
        )


if __name__ == "__main__":
    demo()
Sample Output (Illustrative)
Distance Norzin -> Changzamtog: 1.50 km

Stores within 3 km of Norzin:
 store_norzin at (27.4728, 89.6390) – 0.00 km
 store_changzamtog at (27.4712, 89.6530) – 1.50 km
 store_motithang at (27.4770, 89.6490) – 1.20 km
(Exact distances will vary based on precise coordinates.)

5. Exercises, Use Cases, and Pitfalls
Use Case Extension
For a ride-hailing app:

Store drivers’ locations using keys like geo:drivers:{city}.
For each ride request, search for drivers within 2 km of the passenger’s location.
Students can extend GeoSearchService with methods like:

update_driver_location(driver_id, lon, lat)
find_nearby_drivers(lon, lat, radius_km)
Short Exercises
Add support for searching within a bounding box using GEOSEARCH ... BYBOX.
Integrate the geo-search functionality with the leaderboard, e.g., show top players near a given location (combining sorted sets and geopoints).
Add a method that returns results sorted by distance and limits the top N closest locations.
Common Mistakes
Swapping latitude and longitude (Redis expects longitude first, then latitude).
Assuming geospatial accuracy is suitable for all use cases; remind that Redis geospatial is great for app-level proximity but not for high-precision GIS analysis.
Forgetting to use consistent coordinate systems and units across the application.
Quick Revision Points
Redis geospatial indexes are stored as sorted sets with encoded coordinates.
GEOADD inserts or updates locations, GEODIST measures distance, and GEOSEARCH performs geo-proximity search.
Typical use cases include “nearby stores”, “nearby drivers”, and “events within radius” features in modern mobile apps.
Suggested Lab Activities and Mini-Project Ideas
These activities combine all three practicals and align with DBS302 outcomes. [file:1]

Lab Activity 1 – “Location-Aware Leaderboard”
Combine Practical–1A and Practical–1C: maintain a leaderboard of users and store each user’s last known location in a geo-index.
Implement an API to get “Top 10 players within 5 km”.
This requires intersecting geospatial results with leaderboard ranks, which students can approximate by:
Searching nearby players via geo-index.
Retrieving their scores and manually sorting in the application.
Lab Activity 2 – “Analytics Dashboard”
Use Practical–1B plus others to create a script that:
Tracks DAU and UV per day.
Prints a weekly report on DAU, UV, and average per-day counts.
Optionally, serialize metrics into JSON for further visualization.
Mini-Project Idea – “Redis-Powered Game Backend”
Students design and implement a simplified backend for a mobile game that includes:

Global and country-specific leaderboards.
Real-time analytics of player activity (DAU, UV).
Geo-search for “nearest active game rooms” or “events near you”.
They should focus on:

Clear key design and naming conventions.
Proper encapsulation of Redis operations in classes.
Basic error handling and input validation.
Common Cross-Cutting Mistakes Across All Practicals
Mixing business logic directly with raw Redis calls instead of encapsulating them into clear service classes. This goes against separation of concerns and modular design.
Not using consistent key naming patterns, leading to messy and hard-to-debug data.
Forgetting to clear or namespace keys during development, which can produce confusing results when rerunning demos.
Ignoring error handling (e.g., attempting to compute distance between non-existent members).
Final Quick Revision Summary (All Three Practicals)
Practical–1A: Use sorted sets to maintain rankings with operations like ZADD, ZINCRBY, ZREVRANGE, ZREVRANK. Good for leaderboards and top-N queries.
Practical–1B: Use bitmaps for binary activity (DAU) and HyperLogLog for approximate unique counts (UV) with SETBIT/BITCOUNT and PFADD/PFCOUNT.
Practical–1C: Use geospatial indexes for location storage and geo-search with GEOADD, GEODIST, and GEOSEARCH.
All three are directly tied to Unit II of DBS302 and illustrate how Redis’s advanced data structures enable scalable, real-time features in modern applications.

Last changed by 
 
sarojsanyasi·Follow
0
58
Add a comment
Published on  HackMD
